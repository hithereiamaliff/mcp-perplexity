# Perplexity API Key (for self-hosted mode)
PERPLEXITY_API_KEY=your-api-key-here

# Key Service (for hosted mode — set both to enable)
# When configured, users authenticate with usr_xxx keys resolved via the key service.
KEY_SERVICE_URL=
KEY_SERVICE_TOKEN=
